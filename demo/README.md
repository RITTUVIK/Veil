# Veil Demo App

This folder is reserved for the React demo app that will show two agents:

1. Discover each other by their `.<rootName>` ENS name (e.g. `myagent.veil.eth`)
2. Pay each other using the ENS-resolved agent wallet address

SDK functionality should be implemented under `src/` first.

